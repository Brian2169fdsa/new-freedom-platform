{
  "buildCommand": "npx turbo build --filter=@reprieve/admin",
  "outputDirectory": "dist",
  "installCommand": "npm install --prefix=../..",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
